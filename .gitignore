# Local Word documents
*.docx
~$*.docx
~WRL*.tmp

# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
**/terraform.tfvars
!**/terraform.tfvars.example
# .terraform.lock.hcl is intentionally NOT ignored — it pins provider
# versions/hashes and should be committed for reproducible applies.

# Claude Code — kept out of version control entirely, by request.
.claude/
