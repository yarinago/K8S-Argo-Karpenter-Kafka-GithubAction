# Metadata for the local Helm chart consumed by apps/platform/infra-application.yaml.
# Argo uses this chart plus infra-values.yaml to render Kubernetes manifests.
apiVersion: v2
name: infra
description: Placeholder infra chart managed by Argo CD app-of-apps
type: application
version: 0.1.0
appVersion: "1.0.0"
