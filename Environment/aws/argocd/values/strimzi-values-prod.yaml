# AWS prod Strimzi operator configuration. Same reasoning as
# strimzi-values-dev.yaml — separate cluster, only one namespace to watch.
watchNamespaces:
  - splitwise

livenessProbe:
  initialDelaySeconds: 60
  timeoutSeconds: 10

readinessProbe:
  initialDelaySeconds: 60
  timeoutSeconds: 10
