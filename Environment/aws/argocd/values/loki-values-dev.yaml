loki:
  enabled: true
  # Persistent, unlike local's ephemeral filesystem storage — losing all
  # log history on every pod restart isn't acceptable for a real
  # environment the way it was for a disposable k3d cluster.
  persistence:
    enabled: true
    storageClassName: gp3
    size: 10Gi
  config:
    compactor:
      working_directory: /data/loki/boltdb-shipper-compactor
      shared_store: filesystem
      compaction_interval: 10m
      retention_enabled: true
      retention_delete_delay: 2h
      retention_delete_worker_count: 150
    limits_config:
      retention_period: 720h   # 30 days — default (prod)
      retention_stream:
        - selector: '{namespace="splitwise-dev"}'
          priority: 1
          period: 168h          # 7 days for dev
    chunk_store_config:
      max_look_back_period: 720h
  resources:
    requests:
      cpu: 50m
      memory: 128Mi
    limits:
      cpu: 200m
      memory: 256Mi

promtail:
  enabled: true
  resources:
    requests:
      cpu: 25m
      memory: 64Mi
    limits:
      cpu: 100m
      memory: 128Mi
  config:
    snippets:
      extraRelabelConfigs:
        - action: keep
          source_labels: [__meta_kubernetes_namespace]
          regex: "splitwise.*|infrastructure|argocd"
      pipelineStages:
        - cri: {}
        - regex:
            expression: '(?:^|\s)(?P<level>TRACE|DEBUG|INFO|WARN|ERROR|FATAL|CRITICAL)(?:\s|\[|$)'
        - labels:
            level:

# We already have Grafana from the standalone grafana/grafana release.
# Disable the auto-generated datasource ConfigMap — it sets
# isDefault: true, which conflicts with Prometheus. Loki is added via
# the explicit datasources block in grafana-values-dev.yaml instead.
grafana:
  enabled: false
  sidecar:
    datasources:
      enabled: false
